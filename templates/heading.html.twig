<div class="row">
    <div class="col-sm mb-4">
        <div class="jumbotron">
            <h1 class="display-4">{{ title }}</h1>
            <p class="text-muted">{{ subtitle }}</p>
            <p>{{ form(form) }}</p>
        </div>
    </div>
</div>
